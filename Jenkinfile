pipeline {
      agent any
      stages {
          stage('build') {
              steps {
                 echo "Hello world from gibin"
              }
          }
          stage('test') {
               steps {
                  echo "Test, test world from Gibin"
              }
          }
      }
}
